import { Button } from 'antd'
import styles from '../styles/Home.module.css'

export default function About() {
  return (
    <div className={styles.container}>
      <p>purchase page (stripe / vipps integration... etc!)</p>
      <Button>Confirm Purchase</Button>
    </div>
  )
}
